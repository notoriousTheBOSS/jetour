import "./style.scss";

const Spinner = () => {
    return (
        <div className="min-h-screen absolute top-0 left-0 text-center">
            <div className="loader">Loading...</div>
        </div>
    );
};

export default Spinner;
